package week02classlooppractice;

public class week02classlooppractice {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		// for loops is always 
		//  part 1 - initializer (where the loop starts - starting at 0 or 1, ex) ;
		//part 2 - condition
		//part 3 - increment
		//lines of code you repeat - you can even nest a loop inside a loop! whoa. but tough to keep track of
		
		
		for (int count = 10; count > 0; count = count-1) {
			
			//shorthand count -=1 means count = count-1
			// shorthand count += means count = count+1
			
			System.out.printf("%d...%n", count);
			
			
		}
		System.out.println("BLAST OFF!");
		//non deterministic loop
		//first line initializer on its own line
		//while - condition
		// run the lines of code that repeat
		// increment or decrement (reduction?/diminution)
		//
		
		int count2 = 10;
		while (count2 > 0) {
			System.out.printf("%d...%n", count2);
			count2--;
			
		}
		System.out.println("BLAST OFF!");
		
		
		
		   int num1 = 1;
	        int num2 = 2;
	        int num3 = 3;
	        int num4 = 4;
	    /***** DO NOT CHANGE THE CODE ABOVE THIS LINE *****/
	        
	        // WRITE YOUR CODE HERE
	        if (num1>=num2) {
	        if (num3>=num4) {
	            System.out.println("both statements are true"); {
	        } 
	    }
	}  System.out.println("program ended");

		
	}

}
